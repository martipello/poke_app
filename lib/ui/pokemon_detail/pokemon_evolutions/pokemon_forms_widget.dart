import 'package:flutter/material.dart';

class PokemonFormWidget extends StatelessWidget {
  const PokemonFormWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
